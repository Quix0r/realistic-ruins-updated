local ruins = {
    ["base"] = {},
}

-- base
table.insert(ruins["base"], require("stonering"))
table.insert(ruins["base"], require("scorchmark"))
table.insert(ruins["base"], require("dead-train"))

return ruins
